export default function SpendingIntelligence({ suggestions = [], onDismiss }) {
  return <section className="panel"><div className="panel-head"><div><h2>Spending Intelligence</h2><p className="muted">Review deterministic spending suggestions generated from your household financial data.</p></div></div>{suggestions.length ? <div className="suggestion-list">{suggestions.map((row) => <article className="suggestion" key={row.id}><div><strong>{row.title || row.name || 'Spending suggestion'}</strong>{(row.description || row.message) && <p>{row.description || row.message}</p>}</div>{row.id != null && <button type="button" onClick={() => onDismiss?.(row.id)}>Dismiss</button>}</article>)}</div> : <div className="empty"><strong>No spending suggestions</strong><p>Fynvo will surface useful deterministic spending insights here when enough data is available.</p></div>}</section>;
}
