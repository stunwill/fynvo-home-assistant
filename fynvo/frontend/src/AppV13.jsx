import { useEffect, useRef, useState } from 'react';
import App from './AppCorrectiveV0174.jsx';
import HouseholdControlCenter from './HouseholdControlCenter.jsx';
import LoginPage from './LoginPage.jsx';
import V11ControlCenter from './V11ControlCenter.jsx';
import V13CashFlowPage from './V13CashFlowPage.jsx';
const api=(path,options={})=>fetch(`api${path}`,{credentials:'same-origin',headers:{'Content-Type':'application/json',...(options.headers||{})},...options});
const PRODUCTION_VERSION='1.9.0';
export default function AppV13(){const[auth,setAuth]=useState(null);const[recoveryWarningDismissed,setRecoveryWarningDismissed]=useState(false);const[v11Mode,setV11Mode]=useState(null);const[v13CashFlowOpen,setV13CashFlowOpen]=useState(false);const[householdOpen,setHouseholdOpen]=useState(false);const[householdSecurity,setHouseholdSecurity]=useState(null);const[toolsOpen,setToolsOpen]=useState(false);const observerRef=useRef(null);async function refreshAuth(){try{const response=await api('/auth/state');if(!response.ok)throw new Error('auth');const state=await response.json();setAuth(state);return state;}catch{setAuth({authenticated:false,setup_required:false,user:null,message:'Authentication service unavailable.'});return null;}}async function refreshHouseholdSecurity(){try{const response=await api('/household/me/security');if(!response.ok)throw new Error('security');const state=await response.json();setHouseholdSecurity(state);return state;}catch{setHouseholdSecurity({must_change_password:false,mfa_enabled:false,active_session_count:0});return null;}}useEffect(()=>{refreshAuth();},[]);useEffect(()=>{if(auth?.authenticated)refreshHouseholdSecurity();else setHouseholdSecurity(null);},[auth?.authenticated,auth?.user?.id]);useEffect(()=>{if(!auth?.authenticated)return;const sync=()=>{const footer=document.querySelector('.app-footer');if(footer)footer.textContent=`Fynvo v${PRODUCTION_VERSION}`;};const observer=new MutationObserver(sync);observer.observe(document.body,{childList:true,subtree:true});observerRef.current=observer;sync();return()=>observer.disconnect();},[auth?.authenticated]);const openTool=(mode)=>{setToolsOpen(false);if(mode==='cash-flow')setV13CashFlowOpen(true);else if(mode==='household')setHouseholdOpen(true);else setV11Mode(mode);};if(!auth)return <main className="fynvo-auth-page"><div role="status">Loading Fynvo…</div></main>;if(!auth.authenticated)return <LoginPage authState={auth} onStateRefresh={refreshAuth} onAuthenticated={refreshAuth}/>;if(!householdSecurity)return <main className="fynvo-auth-page"><div role="status">Loading Household identity…</div></main>;if(householdSecurity.must_change_password)return <HouseholdControlCenter forcePasswordChange onPasswordChanged={refreshAuth}/>;if(householdOpen)return <HouseholdControlCenter onClose={()=>setHouseholdOpen(false)}/>;if(v13CashFlowOpen)return <V13CashFlowPage onClose={()=>setV13CashFlowOpen(false)}/>;if(v11Mode)return <V11ControlCenter mode={v11Mode} onClose={()=>setV11Mode(null)}/>;return <>{auth.recovery_mode&&!recoveryWarningDismissed&&<div className="fynvo-recovery-warning" role="status"><span><strong>Administrator recovery mode is enabled.</strong></span><button type="button" onClick={()=>setRecoveryWarningDismissed(true)}>Dismiss</button></div>}<App/><div className="fynvo-tools-menu-shell"><button type="button" className="fynvo-tools-menu-trigger" aria-expanded={toolsOpen} onClick={()=>setToolsOpen((v)=>!v)}>Tools</button>{toolsOpen&&<nav className="fynvo-tools-menu"><button onClick={()=>openTool('cash-flow')}>Cash Flow Intelligence</button><button onClick={()=>openTool('household')}>Household</button><button onClick={()=>openTool('coverage')}>Data Coverage</button></nav>}</div></>;}
